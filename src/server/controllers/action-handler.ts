import PlannedAction from '../../shared/game/planned-action';
import ServerClient from '../objects/server-client';

export function handleAction(client: ServerClient, plannedAction: PlannedAction) {}
